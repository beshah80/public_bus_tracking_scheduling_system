# Backend environment variables
# Replace <db_password> with your actual MongoDB user password
MONGODB_URI=mongodb+srv://beshah_db_user:lpiQMs41gHVMLSGg@tms.7mevfzz.mongodb.net/?appName=tms
# Secret used to sign JWT tokens. Change to a strong unique value in production.
JWT_SECRET=change-this-to-a-strong-random-secret

# CORS: allowed frontend origin
FRONTEND_URL=http://localhost:4028

# Server port (Render provides PORT automatically in prod)
PORT=5000
